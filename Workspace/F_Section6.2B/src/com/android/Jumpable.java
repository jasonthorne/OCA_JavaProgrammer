package com.android;

public interface Jumpable {
	/*
	 * an interface can have variables, but all variables
	 * are, by default
	 * PUBLIC
	 * FINAL
	 * STATIC
	 */
	public final static int min=10;
	//max is also public final and static
	int max=100;
}

interface Moveable{
	String min="ten";
}


