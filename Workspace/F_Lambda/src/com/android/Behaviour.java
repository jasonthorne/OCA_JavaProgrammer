package com.android;

public interface Behaviour {
	/*
	 * this is an abstract that when a class is implementing the
	 * interface will have to override this method
	 * when a lambda overrides this method the lambda will have to have
	 * a parameter list and a body that returns a boolean
	 */
	boolean mad(String myStr);

}
