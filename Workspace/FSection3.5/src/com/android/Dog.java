package com.android;

public class Dog {
	/*
	 * this class has no user defined constructor so this uses the 
	 * DEFAULT CONSTRUCTOR. The default constructor is a hidden
	 * no argument constructor, in the case of the Dog class the
	 * default constructor looks like this
	 * Dog(){
	 * 		super();
	 * }
	 * As soon as we create a constructor ourselves, of any type
	 * we then no longer have access to the above default #
	 * no argument constructor
	 * The default constructor means we can go
	 * Dog spot=new Dog() and a dog will be created, as the command
	 * new Dog() calls the deafult no argument constructor
	 */
	

}
