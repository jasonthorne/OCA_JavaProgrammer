package com.android;

public interface Jumpable {
	
	/*
	 * An interface can have variables, but all varibles are by default:
	 * PUBLIC
	 * FINAL
	 * STATIC
	 */
	
	public final static int min = 10;
	int max = 100; //also public final & static!

}
